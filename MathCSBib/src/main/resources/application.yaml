spring:
  datasource:
    url: jdbc:localhost:5432/mathcsbibdb
    username: username
    password: password
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        current_session_context_class: org.springframework.orm.hibernate4.SpringSessionContext
  resources:
    chain:
      enabled: true
      
sendgrid:
  credentials:
    hostname: smtp.sendgrid.net
    username: username
    password: password
    
server:
  port: 9000

